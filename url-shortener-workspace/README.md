# URL Shortener Workspace

This workspace contains the initial folder structure for a URL shortener project with:

- API service under `url-shortener-api/`
- Data model package under `url-shortener-data-model/`
